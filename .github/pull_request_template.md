## What

<!-- One paragraph: what and why. Reference the issue it closes, if any. -->

## Checklist

- [ ] Tests added/updated in `tests/` (or explained why not needed)
- [ ] `.venv/bin/ruff check src tests` passes
- [ ] `.venv/bin/python -m pytest -q -p no:warnings` passes
- [ ] Results-generating changes updated `results/benchmark.json` + README tables
- [ ] `CHANGELOG.md` updated (and `pyproject.toml` version bumped if this ships a release)
- [ ] No secrets, real financial data, or PII added

## Honest results

<!-- If this changes model behavior, state what improved AND what did not. -->
